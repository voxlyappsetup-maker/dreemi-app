feat(security): Phase 2B — critical API security gate

- Protect GET /api/stories: add authenticateToken, remove userId query trust,
  use req.userId from JWT, verify childId ownership before filtering
- Protect GET /api/stories/:id: add authenticateToken, owner-only check,
  return 404 for both missing and non-owned stories (no existence leak)
  NOTE: public share links intentionally disabled until safe share-token design
- Rate-limit POST /api/stories/generate: 10 req/IP/15 min via express-rate-limit,
  scoped to generate route only, stable JSON error body
- Mistral reliability: AbortController 30s timeout, 1 retry for 429/5xx/network,
  no retry on timeout or 4xx auth errors, removed raw output console.log
- Child safety gate: new safety.service.ts — deterministic rule-based checks
  for sexual/adult, graphic violence, self-harm, hate, weapons instructions,
  drugs, and prompt injection across Arabic, English, and French;
  input checked before Mistral call, output checked before DB write
